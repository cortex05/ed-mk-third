import React from 'react'
// import { useSelector, useDispatch } from 'react-redux'
// import { setActiveCourse, blankActiveCourse } from '../features/course/courseSlice'

export default function HomeScreen() {
  // const dispatch = useDispatch()
  return (
    <div>
      <div>
        HomeScreen
      </div>
      <div>
        <br />
        <label>set Math</label>
        {/*<button onClick={() => dispatch(setActiveCourse("Math"))}>Math</button>
        <br />
        <label>set Science</label>
        <button onClick={() => dispatch(setActiveCourse("Science"))}>Science</button>
        <br />
  <button onClick={() => dispatch(blankActiveCourse())}>Clear</button>*/}
      </div>
    </div>
  )
}